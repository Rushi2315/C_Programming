#include <stdio.h>
#include <conio.h>

int main()
{
    int i=0, Num=0;

    printf("Enter A Number :");
    scanf("%d",&Num);

    for(i=10; i>=; i--)
    {
        printf("%d\n",Num*i);
    }

    getch();
    return 0;
}
